# 測試報告範例

請依自己的執行結果記錄日期、Python 版本、命令、通過數，以及失敗後做了哪些修改。

既有測試涵蓋 400 bytes 記錄、EOF 位置、其他前綴、非數字內容、錯誤輸入、預覽與套用、空批及重跑。新增案例可以從自己最想確認的情況開始，寫出輸入和預期結果。

檢查命令是 `python scripts/verify.py unit`。報告先記錄實際執行過的測試，再執行這個檢查。

本報告記錄 Python 函式的結果。COBOL 編譯、JCL 和 IMS 的執行結果需另行記錄；尚未執行的項目標為待執行。
